services:
  app:
    build: .
    image: simlog:latest
    container_name: simlog
    ports:
      - "8080:5000"
    environment:
      # À CHANGER en production : clé secrète des sessions
      SECRET_KEY: "changez-moi-en-production"
    volumes:
      # Persistance des données (simulations, tickets, audit) hors du conteneur
      - ./data:/app/data
    restart: unless-stopped
