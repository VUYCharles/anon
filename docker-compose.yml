services:
  app:
    build: .
    image: simlog:latest
    container_name: simlog
    ports:
      - "8080:5000"
    environment:
      - SECRET_KEY=${SECRET_KEY}
    volumes:
      # Persistance des données (simulations, tickets, audit) hors du conteneur
      - ./data:/app/data
    restart: unless-stopped
